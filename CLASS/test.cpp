 #include <iostream>
 #include <math.h>

using namespace std;

signed main() {
  float p = 1000, r = 0.05;
  // for (int i = 1; i <= 10; ++i) {
  //   cout << p*pow(1+r, i) << endl;
  // }
  // int i = 0;
  // while (i<10) {
  //   ++i;
  //   cout << p*pow(1+r, i) << endl;
  // }

  return 0;
}
